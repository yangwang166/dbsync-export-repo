{
    "locals": {
        "databricks_secret_scope_cmr_acls_for_each_var": {
            "yang.wang@databricks.com-MANAGE": {
                "permission": "MANAGE",
                "principal": "yang.wang@databricks.com",
                "scope": "${databricks_secret_scope.databricks_secret_scope_cmr.id}"
            }
        }
    },
    "resource": {
        "databricks_secret_acl": {
            "cmr_acls": {
                "for_each": "${local.databricks_secret_scope_cmr_acls_for_each_var}",
                "permission": "${each.value.permission}",
                "principal": "${each.value.principal}",
                "scope": "${each.value.scope}"
            }
        }
    }
}